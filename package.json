{
  "name": "easynpc-rsv-excluder",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "build": "bun build src/index.ts --compile --outfile dist/easynpc-rsv-excluder.exe",
    "compress": "bun run scripts/compress.ts",
    "build:release": "bun run build && bun run compress"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/bun": "latest",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "typescript-eslint": "^8.49.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "smol-toml": "^1.5.2"
  },
  "optionalDependencies": {
    "7zip-bin": "^5.2.0"
  }
}
